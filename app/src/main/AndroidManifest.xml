<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.monstertechno.timely">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ModernDashbord">

        <!-- Main Activity -->
        <activity
            android:name="com.monstertechno.timely.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity
            android:name="com.monstertechno.timely.SettingsActivity" />

        <activity
            android:name="com.monstertechno.timely.FocusActivity" />

        <activity
            android:name="com.monstertechno.timely.NotesActivity" />

        



    </application>

</manifest>
